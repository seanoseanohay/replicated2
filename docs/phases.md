# Phases

## Phase 0 — Project Scaffold ✓ COMPLETE
Goal: Initialize repo and tooling
Deliverables: Project structure, CI/CD, basic API
Success: App runs locally
Built: FastAPI backend, React/Vite frontend, Docker Compose (postgres, redis, minio, backend, worker, frontend), GitHub Actions CI, structlog, Pydantic settings

## Phase 1 — Ingestion ✓ COMPLETE
Goal: Secure bundle upload
Deliverables: Upload, validation, storage
Success: End-to-end ingestion works
Built: POST /api/v1/bundles (multipart, size validation, S3 upload), GET list/detail endpoints, tenant isolation via X-Tenant-ID header, Celery task dispatch, Bundle model + migrations scaffold

## Phase 2 — Parsing ✓ COMPLETE
Goal: Structured evidence
Deliverables: Parsers, normalization
Success: Reliable structured data
Built: Evidence model (JSONB), alembic migration, BundleExtractor (tar.gz→tempdir), parsers for cluster-info/nodes/version, cluster-resources (all k8s kinds + Lists), pod-logs (tail-500, 10MB limit), host-collectors; run_all_parsers registry; GET /api/v1/bundles/{id}/evidence endpoint; 25 tests passing

## Phase 3 — Detection ✓ COMPLETE
Goal: Baseline diagnosis
Deliverables: Rules engine, findings
Success: Evidence-backed output
Built: Finding model, alembic migration 0002, 8 detection rules (node_not_ready, pod_crashloop, oom_killed, image_pull_error, pod_pending, pvc_pending, warning_events, resource_quota), run_all_rules registry, GET+PATCH /api/v1/bundles/{id}/findings, Celery task now runs rules after parsing; 55 tests passing

## Phase 3.1 — Extended Detection (Warnings & Capacity) ✓ COMPLETE
Goal: Catch degraded-but-not-down cluster states
Deliverables: 5 new detection rules, updated registry, new tests
Success: Warning-level issues surface before they become critical
Built: node_pressure (DiskPressure/MemoryPressure/PIDPressure, medium), deployment_unavailable (availableReplicas < desiredReplicas, medium), statefulset_unavailable (readyReplicas < replicas, medium), hpa_maxed (HPA at maxReplicas, medium), warning_event_reasons (FailedScheduling/FailedMount/Evicted/BackOff grouped by reason, high/medium); 9 new tests; 81 tests passing

## Phase 4 — AI Assistance ✓ COMPLETE
Goal: Improve coverage
Deliverables: Retrieval + AI explanations
Success: Improved quality
Built: Anthropic client wrapper, prompt templates, explain_finding() service, POST /api/v1/bundles/{id}/findings/{id}/explain, AI_ENABLED/ANTHROPIC_API_KEY/AI_MODEL config, graceful 503 when disabled

## Phase 5 — Review & Reporting ✓ COMPLETE
Goal: Usable workflows
Deliverables: UI, reports, feedback
Success: Full support workflow
Built: build_report() JSON + build_markdown_report(), GET /api/v1/bundles/{id}/report + /report.md (attachment), frontend FindingCard with severity/status badges + Acknowledge/Resolve/Reopen + AI explanation collapsible + reviewer notes, BundleDetail findings grouped by severity, markdown report download button

## Phase 6 — Hardening ✓ COMPLETE
Goal: Enterprise readiness
Deliverables: Scaling, security, ops
Success: Production-ready system
Built: RequestIDMiddleware (X-Request-ID header + structlog binding), AccessLogMiddleware (method/path/status/duration), magic byte validation (gzip/zip), filename sanitization (path traversal + unsafe chars), slowapi rate limiting (upload 10/min, AI explain 20/min), /health/live + /health/ready (DB+Redis+S3 checks), DELETE /api/v1/bundles/{id} with S3 cleanup, Celery soft/hard time limits (540s/600s) + stuck-bundle cleanup beat task (5min), configurable CORS origins, DB pool settings, non-root Docker user + HEALTHCHECK, Celery beat service in Docker Compose, resource limits on worker (1g/1CPU) and backend (512m/0.5CPU), frontend ErrorBoundary, upload progress bar (XHR); 71 tests passing

## Phase 7 — Authentication & Roles ✓ COMPLETE
Goal: Real user identity, not just X-Tenant-ID header
Deliverables: Login, JWT tokens, role-based access control
Success: Users log in; managers see more than analysts
Built: User model + alembic migration 0003 (email/tenant_id unique constraint), passlib[bcrypt] password hashing, python-jose JWT (access 60min + refresh 30d), POST /auth/register + /auth/login + /auth/refresh + GET /auth/me, get_tenant_id dep (JWT preferred, X-Tenant-ID fallback for backward compat), require_auth + require_manager deps, analyst blocked from resolving findings (403), DELETE /bundles requires manager role, frontend AuthContext (localStorage token, session restore via /auth/me), LoginPage (sign in / create account tabs, dark Tailwind card), Navbar (email + role badge + logout), App.tsx protected routes (redirect to /login if unauthenticated); 90 tests passing

## Phase 8 — Dashboard & Health Overview ✓ COMPLETE
Goal: At-a-glance cluster health across all bundles
Deliverables: Global dashboard, per-bundle health bar, aggregate stats
Success: Green means go, red means wake someone up
Built: GET /api/v1/dashboard (DashboardStats + BundleHealthSummary schemas, compute_health_score helper with critical=-30/high=-15/medium=-7/low=-2 deductions, clamped 0–100, green/yellow/orange/red color bands), tenant-isolated aggregate stats (total_bundles, bundles_ready/processing/error, total_open_findings, findings_by_severity, most_recent_critical up to 5), dashboard router registered in main.py; HealthBar.tsx stacked proportional bar component (red/orange/yellow/blue/gray segments, "All Clear" green when zero), Dashboard.tsx page with 4 summary cards (Total Bundles/Open Criticals/Open Highs/Bundles with Issues), bundle health table (filename, status badge, health bar, score with color, open count, uploaded date, View button), most-recent-critical findings table (manager-only via isManager); dashboardApi.getStats() + BundleHealthSummary/DashboardStats interfaces in client.ts; /dashboard route added to App.tsx, / redirects to /dashboard, Dashboard + Bundles nav links added to Navbar; 8 new tests; 98 tests passing

## Phase 9 — Audit Trail & Finding Events ✓ COMPLETE
Goal: Full history of who did what to every finding
Deliverables: FindingEvent model, history timeline in UI
Success: Compliance-ready audit log; no mystery status changes
Built: FindingEvent model (finding_id, user_id, actor, event_type, old_value, new_value, note, created_at) with composite index on (finding_id, created_at), alembic migration 0004, FindingEventRead schema, auto-record status_changed/note_added events on PATCH /findings/:id, ai_explained event on POST /findings/:id/explain, created event in Celery task after bulk insert, GET /api/v1/bundles/{bundle_id}/findings/{finding_id}/events endpoint with tenant check; collapsible "History" section in FindingCard with colored dot timeline (blue/gray/purple/green), relative timestamps, actor+description display, cached in component state; eventsApi.getEvents() + FindingEvent interface in client.ts; 4 tests passing

## Phase 10 — Notifications ✓ COMPLETE
Goal: Critical findings reach humans without polling the UI
Deliverables: Email + Slack alerts on critical/high findings
Success: On-call gets paged when a node goes NotReady
Built: NotificationConfig model (tenant_id unique, email_enabled, email_recipients, slack_enabled, slack_webhook_url, notify_on_severities, created_at, updated_at), alembic migration 0005, NotificationConfigRead/Update schemas, SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASSWORD/SMTP_FROM/APP_BASE_URL config settings, notifications service (send_email_notification skips if SMTP_HOST empty, send_slack_notification with rich Slack message, notify_bundle_findings filters by severity+status), Celery task calls notify_bundle_findings after run_all_rules wrapped in try/except, GET+POST /api/v1/notifications/config (require_manager), notifications router registered in main.py; NotificationSettings.tsx page with email/slack toggles and recipients/webhook/severities inputs (manager-only guard), /settings/notifications route in App.tsx, Settings link in Navbar (manager-only); notificationApi.getConfig()/updateConfig() + NotificationConfig interface in client.ts; 5 tests passing

## Phase 11 — Comments & Discussion ✓ COMPLETE
Goal: Threaded conversation on findings instead of single notes field
Deliverables: Comment model, comment thread UI
Success: Analyst and manager can collaborate on a finding
Built: Comment model (finding_id+bundle_id FKs with CASCADE, actor, user_id nullable, body Text, created_at, updated_at), alembic migration 0006, CommentRead/CommentCreate schemas (body 1–4096 chars validator), GET/POST/DELETE /api/v1/bundles/{bundle_id}/findings/{finding_id}/comments with tenant check + own-comment-or-manager delete authorization, comments router registered in main.py; Comments section in FindingCard with chat-style thread (actor bold + body + relative timestamp + delete button for own/manager), textarea+submit for new comments, lazy load with state cache; commentApi.list/create/delete() + Comment interface in client.ts; 6 tests passing

## Phase 12 — Bundle Comparison ✓ COMPLETE
Goal: Diff two bundles from the same cluster over time
Deliverables: Comparison view, new/resolved/persisting findings
Success: "What changed since last week?" answered in one click
Built: FindingSummary/ComparisonResult schemas, GET /api/v1/bundles/compare?bundle_a={id}&bundle_b={id} endpoint (placed before /{bundle_id} route to avoid UUID parse conflict) with tenant validation on both bundles, rule_id set diff for new/resolved/persisting, sorted output; BundleCompare.tsx page (/bundles/compare) with two dropdowns for bundle selection, Compare button, 3-column results grid (red/New, green/Resolved, yellow/Persisting) with severity badges, summary counts bar; Compare link in Navbar; comparisonApi.compare() + ComparisonResult/FindingSummary interfaces in client.ts; 5 tests passing

## Phase 2.4 — Replicated Custom Metrics Reporting ✓ COMPLETE
Goal: Report real app activity to Replicated Vendor Portal
Deliverables: metrics_reporter service, APScheduler heartbeat, event-driven emission, manual endpoint
Success: Vendor Portal Instance Details shows bundle counts, findings, and user totals
Built: _send_metrics() via PATCH to Replicated SDK in-cluster API, collect_and_send_metrics_sync() with synchronous DB session querying bundles by status, open findings by severity, and total users; hourly APScheduler job added in backend lifespan (metrics_scheduler_started), event-driven calls after finding status change, bundle deletion, and user registration; POST /api/v1/metrics/report manager-only endpoint; METRICS_ENABLED toggle (default true, false for local dev); removed custom-metrics from Celery beat schedule because workers lack SDK sidecar; verified with mock SDK in kind cluster — payload contains real scalar values only (no nested objects)

## Phase 2.5 — Replicated TLS Configuration ✓ COMPLETE
Goal: Secure HTTPS ingress with multiple certificate options
Deliverables: Self-signed cert persistence, cert-manager support, manual upload path
Success: No cert regeneration on helm upgrade; HTTPS redirect works
Built: tls-secret.yaml using Helm `lookup` function and `helm.sh/resource-policy: keep` to preserve existing self-signed certificates across upgrades; `regenerateSelfSignedCert` toggle to force rotation; `nginx.ingress.kubernetes.io/force-ssl-redirect: "true"` annotation for automatic HTTP→HTTPS redirect; `docs/tls-setup.md` documenting three modes (auto-provisioned cert-manager, manually uploaded secret, self-signed for testing)

## Phase 2.6 — Replicated Update Available Banner ✓ COMPLETE
Goal: Surface pending app updates to end users inside the UI
Deliverables: Update status endpoint, in-memory cache, dismissible banner
Success: Users see a blue banner when a newer release is available; yellow banner when license is invalid
Built: `update_service.py` fetches Replicated SDK `/api/v1/app/info` (current version + license validity) and `/api/v1/app/updates` (available releases) with 5-minute in-memory TTL cache; version comparison by `versionLabel` (available when next != current); `GET /api/v1/updates/status` returns `UpdateStatusRead` (`available`, `version`, `notes`, `license_valid`, `current_version`) with `require_auth` dependency; `UpdateBanner.tsx` mounts inside `ProtectedLayout`, shows blue info banner for available updates, yellow warning banner for invalid license, dismissible via localStorage key per version; 8 backend tests covering no-update, new-version, invalid-license, SDK-unreachable, empty updates list, multiple-updates picks newest, and auth-required

## Phase 2.7 — Optional Ingress ✓ COMPLETE
Goal: Make the Ingress resource optional and off by default so the chart deploys on clusters without an ingress controller
Deliverables: Conditional Ingress template, off-by-default toggle
Success: `helm template` with defaults renders no Ingress; with `ingress.enabled=true` renders a working Ingress routing to the frontend service
Built: `chart/templates/ingress.yaml` wrapped in `{{- if .Values.ingress.enabled }}`; `chart/values.yaml` sets `ingress.enabled: false` by default; Ingress routes `/.Values.ingress.hosts[].paths[]` to `bundle-analyzer-frontend` service on port 3000; supports TLS via cert-manager, manually uploaded secrets, or self-signed certificates (via `tls-secret.yaml` lookup + `helm.sh/resource-policy: keep`); `nginx.ingress.kubernetes.io/force-ssl-redirect: "true"` annotation for HTTP→HTTPS redirect; end-to-end verified with `helm template` (no Ingress with defaults, Ingress present when enabled) and deployed cluster returning HTTP 200 via ingress IP

## Phase 2.8 — Service Type Configurable ✓ COMPLETE
Goal: Allow the frontend service type to be configured so the app is reachable even without an ingress controller
Deliverables: Parameterized service type in frontend and backend service templates
Success: `helm template --set frontend.service.type=NodePort` renders NodePort; `=LoadBalancer` renders LoadBalancer; default remains ClusterIP for use with Ingress
Built: `chart/templates/frontend-service.yaml` uses `{{ .Values.frontend.service.type }}`; `chart/templates/backend-service.yaml` uses `{{ .Values.backend.service.type }}`; `chart/values.yaml` defaults both to `ClusterIP`; all three types verified with `helm template`: default=ClusterIP, NodePort=NodePort, LoadBalancer=LoadBalancer; frontend service targets pod port 3000, backend targets 8000

## Phase 2.9 — Instance Live, Named, Tagged ✓ COMPLETE
Goal: Instance reports as healthy in Vendor Portal, shows custom metrics, and is identifiable
Deliverables: Live instance with custom metrics visible, friendly name, and tags
Success: Vendor Portal Instance Details page shows Ready status, 100% uptime, custom metrics with real app data, named "bundle-analyzer-prod", tagged "environment:production"
Built: Instance `a81dbf1` running in cluster with all 18 resources reporting "ready"; custom metrics sent via `PATCH /api/v1/app/custom-metrics` showing bundles_ingested=2, open findings by severity, total_users=4; instance named "bundle-analyzer-prod" and tagged "environment:production" via Vendor Portal UI

## Phase 2.10 — Services Healthy in Instance Reporting ✓ COMPLETE
Goal: All app services show up as healthy in Replicated instance reporting
Deliverables: SDK app/status endpoint returning all workloads as ready
Success: SDK /api/v1/app/status returns 18 resources (deployments, statefulsets, services, PVCs) all with state "ready"
Built: Replicated SDK auto-discovers and watches all K8s resources in the release namespace; backend, frontend, worker, beat deployments all "ready"; postgresql, redis, minio statefulsets all "ready"; all services and PVCs "ready"; app status shows "Ready" with 100% uptime in Vendor Portal

## Phase 3.1 — Preflight Checks ✓ COMPLETE
Goal: Validate deployment prerequisites before installation to prevent failed installs
Deliverables: troubleshoot.sh preflight spec with required checks, embedded in Helm chart as Secret
Success: Preflights run via kubectl preflight plugin; show passing and failing scenarios with clear, actionable messages
Built: chart/templates/preflights.yaml — Kubernetes Secret with troubleshoot.sh/kind: preflight label containing preflight.yaml spec aligned with official Replicated troubleshoot.sh examples; checks: (1) clusterVersion requires K8s >= 1.25.0, (2) nodeResources requires >= 4Gi memory and >= 2 CPU cores, (3) distribution fails on docker-desktop and microk8s with links to supported options, (4) postgres connectivity only when externalPostgresql configured (conditional via Helm template), (5) textAnalyze on HTTP collector checks Anthropic API reachability, (6) storageClass check only when explicit storageClass is configured (conditional via Helm template) to avoid false failures on clusters using default StorageClass; all failure messages explain what went wrong and how to fix it; verified with kubectl preflight plugin on passing scenario (embedded PostgreSQL, 2 pass + 2 warn = overall PASS) and failing scenario (bad external DB host, DB connectivity FAIL with hostname resolution error = overall FAIL)

## Phase 3.2 — Support Bundle Log Collection ✓ COMPLETE
Goal: Capture logs from every app component for offline troubleshooting, plus runtime health analyzers
Deliverables: troubleshoot.sh support bundle spec with logs collectors and runtime health analyzers, embedded in Helm chart as Secret
Success: `kubectl support-bundle` generates a tar.gz containing non-empty logs for all 8 components and 10 passing analyzer results
Built: chart/templates/support-bundle.yaml — Kubernetes Secret with troubleshoot.sh/kind: support-bundle label containing support-bundle-spec aligned with official Replicated troubleshoot.sh examples; 8 dedicated logs collectors targeting each component by label selector with `limits:` block (maxLines: 10000, maxAge: 720h, maxBytes: 5000000) matching official sample-supportbundle.yaml structure; `clusterInfo` and `clusterResources` collectors scoped to release namespace for privacy; `http` collector to backend `/health/ready` endpoint testing DB+Redis+S3 connectivity; runtime health analyzers: clusterVersion, nodeResources, deploymentStatus (backend/worker/frontend/beat), statefulsetStatus (postgresql/redis/minio), http (backend health endpoint); verified with `kubectl support-bundle` plugin — generated archive contains non-empty logs for all 8 components (backend 2.3MB, frontend 335KB, sdk 202KB, worker 29KB, postgresql 9KB, beat 9KB, redis 1.4KB, minio 504B) and all 10 analyzers report pass including backend health showing `{"status":"ready","checks":{"database":"ok","redis":"ok","storage":"ok"}}`

**Proof of Acceptance Criteria (2026-05-13):**
Run `kubectl support-bundle -n bundle-analyzer --interactive=false`. Output: archive generated, all 10 analyzers pass. Extracted log file listing confirms all 8 major components have non-empty log directories:
```
  2392072  cluster-resources/pods/logs/.../backend.log          ← app (backend)
     9284  cluster-resources/pods/logs/.../beat.log             ← app (beat)
   336166  cluster-resources/pods/logs/.../frontend.log       ← app (frontend)
    29739  cluster-resources/pods/logs/.../worker.log         ← app (worker)
   202522  cluster-resources/pods/logs/.../replicated.log     ← operator (sdk)
      504  cluster-resources/pods/logs/.../minio.log          ← stateful service
     9049  cluster-resources/pods/logs/.../postgresql.log     ← stateful service
     1447  cluster-resources/pods/logs/.../redis.log          ← stateful service
```
Each collector sets `maxLines: 10000`, `maxAge: 720h`, and `maxBytes: 5000000` per official examples.

## Phase 3.3 — HTTP Health Check Collector + Analyzers ✓ COMPLETE
Goal: Collect and analyze the backend health endpoint response to verify runtime dependencies
Deliverables: `http` collector hitting `/health/ready`, `http` analyzer for status code, `textAnalyze` for response body
Success: Support bundle includes health check response and analyzers verify backend can reach DB, Redis, and S3
Built: `http` collector to `http://bundle-analyzer-backend.bundle-analyzer.svc.cluster.local:8000/health/ready` with 10s timeout; `http` analyzer checks `statusCode == 200` (pass) vs `statusCode != 200`/`error` (fail); `textAnalyze` analyzer searches `backend-health.json` for `"status": "ready"` using root-relative path (no `collectorName` required for http collector output); verified in healthy state: both analyzers pass showing backend is ready with DB, Redis, and S3 all accessible; textAnalyze plugin v0.128.1 has known file-path resolution bugs with `runPod`/`exec`/`http`/`logs` collectors when `collectorName` is used — files are placed at bundle root and must be referenced with root-relative paths

## Phase 3.4 — Workload Status Analyzers + Failure Induction ✓ COMPLETE
Goal: Surface which specific workload is down and why it matters operationally
Deliverables: `deploymentStatus` + `statefulsetStatus` analyzers with actionable failure messages
Success: Scaling any deployment to 0 produces a `severity: error` result naming the component and describing user impact
Built: 4 `deploymentStatus` analyzers (backend, worker, frontend, beat) and 3 `statefulsetStatus` analyzers (postgresql, redis, minio); all failure messages include component name + operational impact + remediation step (e.g., "Backend deployment has no ready replicas. The application API is unavailable — users cannot upload bundles, view findings, or interact with the system. Check pod events and logs for crash loops or readiness probe failures."); failure induction demo: scaled backend deployment to 0 replicas → support bundle analysis surfaced `severity: error` with exact message; restored to 1 replica → analysis returned to `severity: debug` pass

## Phase 3.5 — textAnalyze Catches Known App Failure Patterns ✓ COMPLETE
Goal: Search collected log files for application-specific error patterns that indicate real failures
Deliverables: `textAnalyze` analyzer with regex matching known backend error log patterns
Success: Analyzer searches all collected pod logs and would catch failures when they appear
Built: `textAnalyze` analyzer searches `bundle-analyzer-*/*.log` (all app pod logs at bundle root from `logs` collectors) with regex matching 6 known failure patterns extracted from backend source code: `Failed to process bundle`, `bundle_upload_s3_error`, `Failed to reanalyze bundle`, `cleanup_stuck_bundles failed`, `report_custom_metrics failed`, `Metrics collection failed`; verified in healthy cluster: correctly reports "No critical application errors found" (debug) with no false positives; regex correctness proven via synthetic `data` collector test: embedded sample log lines with failure patterns → analyzer fired `severity: error` catching both patterns; also removed `collectorName` from existing backend-health `textAnalyze` so it correctly resolves `backend-health.json` at bundle root

## Phase 3.6 — Storage Class and Node Readiness ✓ COMPLETE
Goal: Verify cluster infrastructure prerequisites: default StorageClass exists and all nodes are Ready
Deliverables: Unconditional `storageClass` analyzer + `nodeResources` node readiness analyzer
Success: Both analyzers report pass on healthy cluster; would fail if no default SC or any node NotReady
Built: Unconditional `storageClass` check with `when: "not exists"` (no `storageClassName` required) — verifies dynamic provisioning prerequisite for PostgreSQL, Redis, MinIO; `nodeResources` check with `nodeCondition(Ready) == False` and `nodeCondition(Ready) == Unknown` outcomes — catches nodes that are down or unreachable despite having sufficient CPU/memory; both verified passing on healthy OrbStack cluster

## Phase 3.7 — Generate Support Bundle from UI + SDK Upload to Vendor Portal ✓ COMPLETE
Goal: Let admin users trigger support bundle generation from the web UI and automatically upload the result to the Replicated Vendor Portal
Deliverables: Celery task for bundle generation + upload, API endpoints for queue/poll status, frontend Troubleshoot page, RBAC for cluster access
Success: Admin clicks "Generate & Upload Support Bundle", polls for completion, sees Vendor Portal link
Built: Celery task `generate_and_upload_support_bundle()` runs `kubectl-support_bundle` plugin (v0.128.1) against a Secret wrapper containing the troubleshoot spec (raw CRD apply fails without `troubleshoot.sh/v1beta2` CRDs on standard clusters); task streams output to structlog, uploads generated tar.gz via Replicated SDK `POST /api/v1/support-bundles` (cluster-aware via `REPLICATED_SDK_HOST` env); API endpoints `POST /api/v1/support-bundles` (returns `{task_id, status, message}`) and `GET /api/v1/support-bundles/{task_id}` (returns `{task_id, status, result}` with `result.status`, `bundle_id`, `slug`); both endpoints use `require_admin` dependency; `support_bundles_router` registered in `main.py` with correct `/api/v1/support-bundles` prefix (fixed from `/support-bundles` for API consistency); frontend `Troubleshoot.tsx` page with admin-only guard (`isAdmin`), "Generate & Upload" button, 3-second polling for task status, green success banner with Bundle ID and Vendor Portal link; `supportBundleApi` in `client.ts` with `generate()` and `getStatus()`; admin-only "Troubleshoot" nav link in `Navbar.tsx`; `/troubleshoot` route in `App.tsx`; RBAC: `bundle-analyzer-backend` ServiceAccount + `bundle-analyzer-bundle-collector` ClusterRole (read-only on 40+ resource types) + ClusterRoleBinding; both backend and worker pods use same SA; backend/worker/beat images include kubectl v1.32.0 + support-bundle v0.128.1 plugin; ingress updated with `/api` path routing to backend service so frontend SPA can reach API; verified end-to-end: POST through frontend proxy → task queued → worker generated bundle (4s) → uploaded to Vendor Portal → polled status returned `{"status":"uploaded","bundle_id":"3Dicez1lTxiBPFLxmhTL5i0CuTI","slug":"2026-05-14@14:20"}`; 220 tests passing

## Phase 4.1 — EC v3 Install on Bare VM ✓ COMPLETE (RE-DONE WITH REAL EC v3)
Goal: Install Bundle Analyzer on a bare Ubuntu VM using Replicated Embedded Cluster v3
Deliverables: EC v3 Config manifest with correct version format, HelmChart v2 CR, Application CR, Config CR, `.helmignore`, multi-arch images
Success: App deploys via EC v3 headless installer, all pods Running, backend healthy
Built: EC v3 Config manifest (`embedded-cluster-config.yaml`) with version `3.0.0-beta.4+k8s-1.34` (fixed from `3.0.0-beta.4` — missing `+k8s-1.34` suffix caused air gap build failure), HelmChart v2 CR (`helmchart.yaml`) with `chartVersion: 0.1.8`, Application CR (`application.yaml`), Config CR (`config.yaml`) with minimal hostname field (required for EC v3 headless install), `.helmignore` to prevent chart bloat, multi-arch Docker images; release 41 (`0.1.8`) promoted to EC-Test channel; CMX VM `873460b1` provisioned (Ubuntu 24.04, x86_64); EC v3 installer bundle downloaded (`curl https://replicated.app/embedded/bundle-analyzer/ec-test`), extracted, installed headlessly with `sudo ./bundle-analyzer install --license license.yaml --headless --yes --installer-password 'Admin123!'`; required runtime fixes on VM: (1) patched `enterprise-pull-secret` to add `images.bundlyzer.com` auth domain (generated secret only had `proxy.replicated.com` and `registry.replicated.com`), (2) patched all app ServiceAccounts (`bundle-analyzer-backend`, `bundle-analyzer-sdk`, `default`) with `imagePullSecrets: enterprise-pull-secret`, (3) created `bundle-analyzer-license` secret with both `config.yaml` key (for SDK volume mount `subPath: config.yaml`) and `integration-license-id` key (for SDK env var), (4) deleted pods to force recreation with updated secrets; all pods now Running: backend, worker, beat, frontend, sdk, postgresql, redis, minio; backend `/health/ready` returns `{"status":"ready","checks":{"database":"ok","redis":"ok","storage":"ok"}}`; frontend serving Bundle Analyzer HTML

## Phase 4.2 — In-Place Upgrade Without Data Loss ✓ COMPLETE
Goal: Deploy a new release to an existing EC v3 cluster without losing application data
Deliverables: Visible frontend change, new chart version, new image tag, release promoted, update deployed via Admin Console
Success: New version deploys, frontend shows visible change, all pods healthy, PostgreSQL data intact
Built: Frontend Navbar updated with `v0.1.2` version badge, chart version bumped to `0.1.2`, HelmChart v2 CR `chartVersion` bumped to `0.1.2`, frontend image tag pinned to `0.1.2` (not `latest`) to force deployment spec change, multi-arch frontend image built and pushed with tag `0.1.2`, release 32 created and promoted to EC-Test channel; update check triggered via Admin Console API (`POST /api/v1/app/bundle-analyzer/updatecheck`) detected new version 0.1.2, deployment triggered (`POST /api/v1/app/bundle-analyzer/sequence/1/deploy`), rolling update completed in ~25s with only frontend pod replaced (other pods unchanged), new frontend pod confirmed using `0.1.2` image, JS bundle contains version badge string, PostgreSQL StatefulSet never restarted (PVC intact), backend health check returns Ready, app status Ready; friction: Helm SSA conflicts from 4.1 manual patches (`kubectl patch` on `enterprise-pull-secret` and SDK deployment) caused Admin Console to show "failed" status in version history despite successful deployment and functional app; lesson: manual `kubectl patch` creates upgrade debt — always reconcile patches into the chart

## Phase 5.1 — Configurable App Feature via Config Screen (≥2 non-trivial features) ✓ COMPLETE
Goal: Two non-trivial features wired through the EC v3 config screen (KOTS `Config` CRD rendered by install wizard, not KOTS Admin Console) such that flipping the toggle visibly changes app behavior
Deliverables: Second config item in `config.yaml`, Helm wiring in `helmchart.yaml`, chart values plumbing, backend gate, frontend gate, tests; companion to 4.7's AI Chat feature
Success: With `allow_registration=true` Create Account tab shows and `POST /auth/register` succeeds; with `allow_registration=false` the tab is hidden and the endpoint returns 403; same install can be reconfigured without redeploy
Built: Feature 1 (existing, from 4.7) — `enable_ai_chat` config item gated by license `ai_chat_enabled`; `helmchart.yaml` wires `backend.aiEnabled` via `ConfigOptionEquals "enable_ai_chat" "1"`; backend middleware blocks `/api/v1/findings/explain` when off. Feature 2 (new) — `allow_registration` bool item in `config.yaml` "General" group with help_text; `helmchart.yaml` adds `global.allowRegistration: '{{repl ConfigOptionEquals "allow_registration" "1" }}'`; `chart/values.yaml` adds `global.allowRegistration: true`; `chart/templates/backend-configmap.yaml` projects to env `ALLOW_REGISTRATION` via `ternary` on the bool; backend `core/config.py` exposes `Settings.ALLOW_REGISTRATION` (default `True` for local dev); `backend/app/api/routes/auth.py` returns 403 "Registration is disabled" from `/api/v1/auth/register` when off; new unauthenticated `backend/app/api/routes/public_config.py` returns `{allow_registration: bool}` from `GET /api/v1/config` (added to `_LICENSE_ALLOWLIST` in `main.py` so it works before sign-in); frontend `appConfigApi.getPublicConfig()` in `client.ts`; `LoginPage.tsx` fetches the flag on mount, hides the Create Account tab when false, force-redirects an open register tab back to sign-in if the flag flips, and shows an explanatory "Account creation is disabled for this install" notice; tests in `backend/tests/test_public_config.py` cover both the public endpoint reflecting the flag and the register endpoint returning 403 when off (passing locally). Verification still required: (a) cut chart release on EC-Test channel, (b) install on fresh CMX VM in online (non-air-gap) mode, (c) confirm Create Account visible with default-on, (d) reconfigure to off via EC admin console and confirm tab disappears + 403 from `/auth/register`. Status will flip to ✓ COMPLETE after end-to-end verification.

Verified end-to-end (2026-05-19): (a) Release 0.3.25 cut, promoted to both Unstable and EC-Test channels via updated CI workflows; (b) installed on CMX VM in online (non-air-gap) mode and upgraded in-place from 0.3.22 → 0.3.24 → 0.3.25 without VM rebuild; (c) Feature 2 (allow_registration) confirmed earlier in friction log 15. Feature 3 (notification defaults — install-time email/slack recipients/severities via EC config) added as a third verification: EC config value `default_email_recipients=test@email.com` flowed through helmchart.yaml → ConfigMap → backend env → DB hydrate on backend startup → in-app Notifications page auto-populated the Email Recipients field. Bug fixes shipped to make this work reliably: (1) `_bootstrap_notification_defaults` in `app/main.py` changed from one-shot create-or-skip to create-or-hydrate-empty-fields so late-arriving install-time defaults reach the DB on subsequent restarts; (2) `.github/workflows/replicated-release{,-main}.yml` updated to promote each release to both `Unstable` and `EC-Test` (was hardcoded to `Unstable` only — left the EC-Test-subscribed dev VM stranded at older versions). See `friction-logs/week2/17-notification-config-seeding-and-ci-channel-gap.md` for the full diagnosis and root cause. Still open: backend pod has no `SMTP_*` env vars, so notification *configuration* works end-to-end but emails won't actually send until SMTP is wired (tracked separately).

## Phase 5.2 — Generated Default Value Survives Upgrade ✓ COMPLETE
Goal: Auto-generated install-time secret (e.g., JWT signing key) persists across chart upgrade without reconfiguration, and application state depending on that secret (issued JWTs) remains valid post-upgrade.
Deliverables: Chart template using `lookup` to read existing Secret, `randAlphaNum` fallback for first install, `helm.sh/resource-policy: keep` to survive `helm uninstall`, deployment wiring via `envFrom`, end-to-end verification on a real EC v3 cluster.
Success: A JWT issued under release N is still valid after upgrading to release N+1; SHA256 hash of `JWT_SECRET_KEY` is byte-identical pre- and post-upgrade.
Built: Picked JWT secret over the rubric's canonical DB-password example because it's app-controlled (not Bitnami-inherited), has a high-signal failure mode (all sessions invalidated on rotation), and turns a real security gap (publicly-known hardcoded `dev-jwt-secret-change-in-prod` in `values.yaml`) into a fix. Implementation split `chart/templates/secret.yaml` into two files with explicit contracts: `secret.yaml` (templated, rewritten every upgrade — DATABASE_URL, REDIS_URL, MinIO creds, bootstrap admin) and `app-secrets.yaml` (new — lookup-first, generate-only-if-missing, `helm.sh/resource-policy: keep`). Resolution order per nullable field: (1) existing Secret has value → reuse; (2) `--set` override → use; (3) generate `randAlphaNum 64`. Deployments (`backend-deployment.yaml`, `worker-deployment.yaml`, `beat-deployment.yaml`) now `envFrom` both Secrets. `values.yaml` defaults blanked for `secretKey`/`jwtSecretKey`. Verified end-to-end (2026-05-20) on VM `df9dc499`: 0.3.28 install created `bundle-analyzer-app-secrets` with random JWT (SHA256 `e09eb07ed235395aa269d0d53e99d3e3b53df6dc087d14fd6646bf5b98ddd097`); captured a token via `/api/v1/auth/login`; upgraded in place to 0.3.29; same hash byte-identical AND same token still validates against `/api/v1/auth/me`. Two real bugs caught and fixed in flight, both captured in `friction-logs/week2/18-secret-split-shipping-bugs.md`: (1) `IsUpgrade` guard intended to catch silent JWT rotation also fired on the legitimate first-introduction-of-template case (dropped); (2) backend-deployment.yaml had stale explicit `env:` entries that valueFrom-referenced `SECRET_KEY`/`JWT_SECRET_KEY` from the *old* Secret name, causing `CreateContainerConfigError` because those keys were now in the new Secret (removed; envFrom does the work).

## Phase 5.3 — Input Validation on Config Items ✓ COMPLETE
Goal: A KOTS Config item with regex validation that blocks invalid input and accepts valid input as the customer sees it in the EC v3 config screen.
Deliverables: At least one Config item with `validation.regex.pattern + .message`, verified end-to-end on a real EC v3 install.
Success: Invalid input triggers the validation message; the affected group shows "Incomplete"; save is blocked. Valid input is accepted.
Built: Two regex validators added in chart 0.3.30 on the External PostgreSQL Connection group: `external_postgresql_port` (pattern `^([1-9][0-9]{0,4})$`, message "Must be a positive integer between 1 and 65535...") and `external_postgresql_host` (DNS-style pattern `^[a-zA-Z0-9]([a-zA-Z0-9.-]{0,253}[a-zA-Z0-9])?$`, message "Must be a valid hostname or IP address..."). Verified on VM `3eed4e13` 2026-05-20: entering `fawef` in the Port field surfaced the exact validation message inline under the field; left nav showed the External PostgreSQL Connection group with a red "Incomplete" badge; save blocked. Valid inputs (`5432`, `db.internal.example.com`, `10.0.1.42`) accepted. Per friction log 28 in the bootcamp example, EC v3 validates at submit time rather than per-field — the demo flow is "click Save, error surfaces on the affected group, navigate back, fix, save again." Sufficient for the rubric. See `Onboarding/Bootcamp/5.3.md` for full proof and screenshot reference.

## Phase 5.4 — Help Text on Every Config Item ✓ COMPLETE
Goal: Every user-facing Config item has `help_text` describing what the field does and what a valid value looks like — not just restating the field label.
Deliverables: 100% help_text coverage across `config.yaml`, with each entry following the "function + valid format + example" template.
Success: Automated audit reports zero items missing help_text; visual confirmation in the EC config screen shows the help text rendered under each field with substantive content.
Built: Audited `config.yaml` at 0.3.29 — 11 items had help_text (some thin, e.g., `external_postgresql_port: "PostgreSQL TCP port"` restated the label) and 4 items had none (`hostname`, `external_postgresql_username/password/database`). Rewrote thin entries and added missing ones in chart 0.3.30. Each help_text now follows: (1) one or two sentences on what the field does and where it shows up in the running app, (2) a `Valid format:` or `Valid values:` line with concrete examples (e.g., `db.internal.example.com`, `5432`, `critical,high`). The two new hidden `embedded_postgresql_*_password` items added by 5.2 also have help_text per this audit. Final coverage: 15 of 15 items, confirmed via `python yaml.safe_load` parsing the file and iterating. Visual confirmation captured from VM `3eed4e13` 2026-05-20: External PostgreSQL Connection group shows full help text under each field including format examples. See `Onboarding/Bootcamp/5.4.md` for the audit table and template.

## Phase 5 — Friction summary

Three friction-log entries shipped from the 5.x tier:

- `friction-logs/week2/17-notification-config-seeding-and-ci-channel-gap.md` — Phase 5.1, two issues (one-shot bootstrap seed, CI promoted to Unstable only).
- `friction-logs/week2/18-secret-split-shipping-bugs.md` — Phase 5.2 chart-template path (IsUpgrade guard false-positive, stale explicit env refs to moved Secret keys).
- `friction-logs/week2/19-helmchart-yaml-lint-vs-runtime-template-form.md` — Phase 5.2/5.3/5.4 release, the `{{repl}}` vs `repl{{}}` YAML-lint trap.

All five tasks in Tier 5 (5.0–5.4) are now ✓ COMPLETE.

## Phase 6.1 — Enterprise Portal Branding ✓ COMPLETE
Goal: Replace the Replicated default chrome on the customer-facing Enterprise Portal with custom Bundle Analyzer branding so a customer arriving at the EP sees a coherent product experience.
Deliverables: Logo, favicon, page title, primary + secondary colors configured in Vendor Portal → Enterprise Portal → Portal Settings; PNG assets committed to the repo so the brand chain is reproducible; single screenshot of the live EP showing all four artifacts at once.
Success: Browser tab favicon, header logo, page title, button color, and link color on the live EP all match the in-app brand and are demonstrably non-default.
Built: 2026-05-20 — generated `frontend/public/logo-96.png` (96×96) and `frontend/public/favicon-32.png` (32×32) from the existing `frontend/public/favicon.svg` using `qlmanage -t -s <N> -o /tmp favicon.svg && cp /tmp/favicon.svg.png ./<name>.png` (macOS Quick Look, no homebrew/node dependency). Uploaded both to Vendor Portal → bundle-analyzer → Enterprise Portal → Portal Settings; set Title="Bundle Analyzer — Enterprise Portal", Page overview="AI-assisted Kubernetes support bundle analysis for your team.", Contact email="support@bundlyzer.com", Background=Minimalist, Primary color=`#6366f1` (Tailwind indigo-500, matches navbar text), Secondary color=`#10b981` (Tailwind emerald-500, matches navbar version badge). Verified at `https://enterprise.replicated.com/bundle-analyzer/login` — single screenshot captures all four rubric artifacts: hexagon logo in card header, hexagon favicon in browser tab, custom title rendered in card heading and browser tab title, primary color on the "Continue with email" button, secondary color on the "Sign up for your 30 day trial" link. Rubric proof at `bootcamp-screenshots/6.1-enterprise-portal-branding.png`. The Preview widget on the Colors section made it easy to confirm color wiring before saving — both swatches rendered with their actual roles ("Button" + "Link") before the live EP was visited.

## Phase 6.2 — Enterprise Portal Custom Email Sender ✓ COMPLETE
Goal: Outbound invitation/notification emails from the Enterprise Portal arrive at customers with a `From:` address on the vendor's custom domain (`bundlyzer.com`), not Replicated's domain.
Deliverables: DKIM TXT and Return-Path CNAME DNS records added to `bundlyzer.com` via Namecheap; Vendor Portal verification turned green; real invitation email received in a real inbox with the expected From-address; rubric-compliant screenshot of the received email.
Success: Email lands in recipient's inbox with `From: noreply@bundlyzer.com`, DKIM signature passes, recipient mail server accepts the message without spam flag.
Built: 2026-05-20 — first had to unblock the feature: the Customer Emails configuration UI is hidden behind a Grant-type feature flag in the internal Replicated admin tool ("Enable Customer Emails in Enterprise Portal (Grant)"). Bootcamp rubric implies this is self-serve in Vendor Portal but it is not; mentor confirmed and pointed at the flag. Documented as `friction-logs/week2/20-customer-emails-behind-feature-flag.md` with explicit critique of the misleading instructions. After the flag was granted: navigated to bundle-analyzer → Enterprise Portal → Customer Emails tab (newly visible); set From email address to `noreply@bundlyzer.com`; clicked Configure DKIM; received two DNS records to add — DKIM TXT at `20260520182509pm._domainkey.bundlyzer.com` (full RSA pubkey value) and Return-Path CNAME at `pm-bounces.bundlyzer.com` → `pm.mtasv.net` (Postmark). Added both in Namecheap Advanced DNS (Namecheap auto-appends domain so Host fields were entered as `20260520182509pm._domainkey` and `pm-bounces` respectively); existing SPF for Namecheap email forwarding left untouched (Postmark uses DKIM-only auth, no SPF merge required); existing `images` CNAME for Replicated image proxy untouched. Verified propagation with `dig +short TXT` and `dig +short CNAME` (TXT live in <30s, CNAME in ~60s); clicked Verify DNS Records in VP — all three wizard steps green: Set email address, Verify domain, Complete. Triggered a real invitation by inviting the bootcamp test user to `lawrence@replicated.com` from a test customer on the bundle-analyzer channel. Email received in Gmail within ~10 seconds with `From: noreply@bundlyzer.com`, Gmail "External" label confirming sender domain distinct from recipient, DKIM authentication implicitly passed (Gmail did not flag the message). Rubric proof at `bootcamp-screenshots/6.2-email-sender-configured.png` (Vendor Portal verified state) and `bootcamp-screenshots/6.2-gmail-invitation.png` (received email).

## Phase 6.3 — Enterprise Portal Security Center ✓ Part 1 complete · 🚫 Part 2 blocked (structural gap)
Goal: Customer logs into the Enterprise Portal, visits the Security Center, sees real vulnerability data on the running application; if vulns are present in the application image, rebase on SecureBuild language-level base images to reduce CVE counts.
Deliverables (Part 1): Security Center feature flag granted; vendor- and customer-side Security Center surfaces working; real CVE data displayed; downloadable SBOM; rubric screenshot.
Deliverables (Part 2): N/A — see blocker below.
Success (Part 1): Customer-side Security Center tab visible in EP; shows CVE counts, per-image breakdown, downloadable SPDX SBOM.
Success (Part 2): Customer rebases application Dockerfile(s) on SecureBuild language-level images; demonstrates before/after CVE reduction across a new release.
Built (Part 1, 2026-05-20): Asked Replicated mentor (Dmitriy Ivolgin) to grant the "Enable Security Center in Enterprise Portal (Grant)" feature flag on team `notcoffee` in the internal admin tool (`admin.replbiz.com`); flag flipped instantly. Hard-refreshed Vendor Portal; new `Security` left-nav entry appeared for bundle-analyzer with Grype-scanned vulnerability data across the release history. Release 0.3.32 (sequence 92 on EC-Test) showed 349 total vulnerabilities — 14 critical / 102 high / 192 medium / 41 low — across 6 of 9 container images (the 3 clean ones are Bitnami postgres + redis on Photon OS, and replicated-sdk which already uses SecureBuild). Enabled the customer-side Security Center surface; logged into `enterprise.replicated.com/bundle-analyzer` as the test customer; the EP Security Center tab rendered the same data plus the CVE Report panel, Software Bill of Materials panel, downloadable SBOM (1058 packages, 19526 files, SPDX 2.3, tooled by "SecureBuild SBOM Merger"), and a Container Vulnerabilities table. Saved customer-side screenshot to `bootcamp-screenshots/6.3-customer-security-center.png` and downloaded SBOM to `bootcamp-artifacts/6.3-sbom-0.3.32.json` (17.8 MB).
Blocked (Part 2): The rubric requires rebasing application images on "language level securebuild image[s]" to reduce CVEs. Mentor confirmed verbatim ("nobody can use a securebuild image except for embedded cluster and kots images, which we have made public via registry.replicated.com" / "securebuild registry is not publicly accessible for anybody") that the SecureBuild registry is structurally non-public for customers. The only SecureBuild-built images any customer can pull are the ones Replicated re-publishes for its own products (KOTS, EC, replicated-sdk, troubleshoot). There is no SecureBuild Python, nginx, or Node image a vendor can rebase application Dockerfiles on today — and per Dmitriy "we are the customer in this case" so the structural shape is permanent for our role. Mentor's explicit instruction for the gap: "friction log." Captured at `friction-logs/week2/21-securebuild-language-images-not-available.md`, including verbatim Slack thread and the corrected three-concept disambiguation (SecureBuild the project / SecureBuild the registry / Replicated product images built with SecureBuild). For real production use, equivalent customer remediation paths are: Chainguard Images (closest direct analog), Google distroless, Bitnami hardened bases, DIY SecureBuild build pipeline (open source), or accept-and-document. Dockerfile diff prepared and stored in 6.3.md ready to apply against whichever option is chosen post-bootcamp.

## Phase 6.4 — Enterprise Portal Custom Setup Instructions ✓ COMPLETE
Goal: Connect a GitHub content repo to the Enterprise Portal, customize the left nav and main content with installation and operating instructions specific to Bundle Analyzer.
Deliverables: Content repo linked via the Replicated GitHub App; custom `toc.yaml` with new Operations group; product-specific install pages (Helm + Embedded Cluster); three new Operations pages (Day-2, Backups, Troubleshooting); product-specific Security page wiring 6.3's MDX components; branding mirrored from 6.1 into `theme.yaml`; per-customer Portal Version toggle flipped so the New EP renders for the customer; rubric screenshot of the customer-facing New EP.
Success: Customer at `bundle-analyzer.enterpriseportal.app/<version>` sees Bundle-Analyzer-specific left sidebar nav with the Operations group, product-specific install instructions, and operating-instructions pages with concrete commands referencing Bundle Analyzer's actual service names and CLI.
Built (2026-05-20): Granted three feature flags in the internal admin tool — `Enterprise Portal Customization (Marc)`, `GitHub App Integration in Vendor Portal (Marc)`, plus the already-granted `Enable Customer Emails (Grant)` (6.2) and `Enable Security Center (Grant)` (6.3). Created `seanoseanohay/bundle-analyzer-enterprise-portal-content` from Replicated's template via Vendor Portal Content tab → Create Repo from Template. Installed the Replicated GitHub App on `seanoseanohay` org with all-repos scope (preempts the 6.6 Terraform repo addition). Linked the repo; Vendor Portal validated `toc.yaml` at the root of `main` and marked it as Fallback. Cloned to `~/onboarding/bundle-analyzer-enterprise-portal-content/`; customized: added `assets/logo.png` (96×96 from `frontend/public/favicon.svg`) + `assets/favicon.png` (32×32); filled in `theme.yaml` with 6.1 branding (Bundle Analyzer title, overview copy, indigo/emerald colors, support contact, login footer); added `Operations` group to `toc.yaml` with three new pages (`pages/operations/day-2.md`, `backups.md`, `troubleshooting.md`); rewrote `pages/home.md`, `pages/installation/helm.md`, `pages/installation/linux.md`, and `pages/security.md` with Bundle-Analyzer-specific content referencing the actual service names (`bundle-analyzer-backend`, `bundle-analyzer-postgresql-0`, etc.), real env vars (`BACKEND_AI_MODEL`, `REPLICATED_SDK_HOST`), the EC v3 binary path `sudo /usr/local/bin/bundle-analyzer shell`, and concrete failure modes from our friction-log work (5.2's Postgres password contract, image-pull-policy on `:latest`). Pushed commit `f0c2ce7`; auto-sync via webhook hit Vendor Portal within seconds. For customer-side verification, discovered that the `New Portal | Classic Portal` toggle at the top of the Vendor Portal EP admin page is an admin-side preview switch only (per `docs.replicated.com/vendor/enterprise-portal-v2-about`); customer routing is per-customer at `Customers → <customer> → Enterprise Portal access → Portal Version (Alpha)`. Flipped that toggle on `bundlyzer-dev-test`, also enabled Security Center access for that customer. Hit a team-policy validation error ("this team cannot create customers with kurl install enabled") because the customer had kURL install method enabled at creation and the New EP only supports Helm + Embedded Cluster per documented limitations; resolved by editing the customer's Install Methods to remove kURL and KOTS. The New EP lives at `bundle-analyzer.enterpriseportal.app` (not the `enterprise.replicated.com/{slug}` Classic URL). Verified by visiting `https://bundle-analyzer.enterpriseportal.app/0.3.32` in a fresh incognito and logging in as the test customer — left sidebar nav shows our exact `toc.yaml` structure (Installation / Operations / Updates / Security / Support); home page renders "Welcome to Bundle Analyzer" with our product description; both Linux and Helm install methods listed based on the customer's license entitlements. Saved as `bootcamp-screenshots/6.4-new-portal-customer-view.png`. Captured the routing-discovery friction at `friction-logs/week2/22-epv2-customer-routing-undocumented.md`.

## Phase 6.5 — Helm Chart Reference in EPv2 ✓ COMPLETE
Goal: Customer visiting the Enterprise Portal sees an auto-generated Helm chart reference page driven by the chart's values, with at least one field documented uniquely in the reference (not duplicated from README or other docs).
Deliverables: `helm_chart` content-type entry in the content repo's `toc.yaml`; inline `# ...` comments on the priority fields in `chart/values.yaml`; chart bumped so the EP fetches the updated values; customer-side verification screenshot.
Success: Reference page renders with prose descriptions (not AI-fallback "Backend max bundle size mb" titlecase) for the documented fields; `backend.maxBundleSizeMb` description spells out the memory-vs-bundle-size tradeoff that doesn't exist anywhere else.
Built (2026-05-20): Added a `Reference` group with a `Helm Chart Reference` entry to `toc.yaml` using the `helm_chart: name: bundle-analyzer` syntax confirmed from `docs.replicated.com/vendor/enterprise-portal-v2-content`. First-pass implementation in chart 0.3.33 added ~38 descriptions to `chart/values.schema.json` as JSON Schema `description:` properties; the EP renderer ignored them and used AI-fallback titlecase descriptions (e.g., "Backend max bundle size mb") because the actual documented source for descriptions is inline `# ...` comments above each field in `values.yaml`, per `docs.replicated.com/vendor/enterprise-portal-v2-helm-reference`. Reshipped in chart 0.3.34 by adding the same ~38 prose blocks as inline comments in `values.yaml` while keeping the JSON Schema descriptions in place for IDE/CLI tooling. Used `# @docs:skip` to hide `replicatedLicenseFields` from the customer reference (platform-injected, not customer-tunable). Used `# @docs:internal` on `ingress.regenerateSelfSignedCert` (deprecated permanently-ignored field). Verified at `bundle-analyzer.enterpriseportal.app/0.3.34/content/reference/helm/bundle-analyzer` — `backend.maxBundleSizeMb` now renders with the full memory-vs-bundle-size guidance, including markdown formatting (backticks) and multi-sentence prose. Screenshot saved to `bootcamp-screenshots/6.5-helm-chart-reference.png`. The "uniquely documented here" rubric criterion is satisfied by `backend.maxBundleSizeMb` — the operational guidance for when to lower it on memory-constrained VMs isn't documented in README, NOTES.txt, or anywhere else in the codebase.

## Phase 6.6 — Terraform Modules in EPv2 ✓ COMPLETE
Goal: Surface a customer-facing Terraform module reference page in the Enterprise Portal so platform-team customers who manage their infra in Terraform can pull a Bundle Analyzer install module without leaving their existing IaC workflow.
Deliverables: New GitHub repo `seanoseanohay/bundle-analyzer-terraform-modules` linked through the Vendor Portal Enterprise Portal → Terraform tab; `terraform_module` entry in the content repo's `toc.yaml` so EP renders the docs page; per the rubric note (*"fake module sufficient"*), the module is syntactically valid but not end-to-end functional (missing pull-secret wiring + multi-cloud provider setup), with documentation framed honestly to that effect.
Success: Customer-side EP at `bundle-analyzer.enterpriseportal.app` shows a "Terraform Module — Helm Install" entry under Reference, the page renders the parsed `variables.tf` / `outputs.tf` / `README.md`, and a new GitHub repo exists with a `v0.3.35` git tag matching the chart version.
Built (2026-05-21): Got the "Terraform Modules in Enterprise Portal (Marc)" feature flag granted on the internal admin tool (fifth alpha flag this team has needed for tier 6 — pattern is now well-established: customer-visible alpha features all live behind admin.replbiz.com grants that aren't self-serve from the Vendor Portal). Scaffolded `seanoseanohay/bundle-analyzer-terraform-modules` locally under `~/onboarding/bundle-analyzer-terraform-modules/` with the layout `modules/helm-install/{main,variables,outputs,versions}.tf + README.md`. Module wraps a single `helm_release` resource pulling `oci://registry.replicated.com/bundle-analyzer/stable` chart `bundle-analyzer` version `var.chart_version`. Inputs cover `license_id` (sensitive), `chart_version`, `namespace`, `release_name`, `ai_chat_enabled`, `postgresql_mode` (embedded/external + validation), full external Postgres connection details, ingress enable + hostname — 12 inputs total. Outputs: `namespace`, `release_name`, `chart_version`, `service_endpoint`. README documents the proxy-registry source form (`proxy.replicated.com/bundle-analyzer/bundle-analyzer-terraform-modules/github`) plus full provider-config example for EKS. Pushed to GitHub as private repo with initial commit `7bfe200` and tag `v0.3.35` (matches current chart version per the docs requirement: "Release version labels must match git tags with a `v` prefix"). Updated content repo `bundle-analyzer-enterprise-portal-content/toc.yaml` to add `Terraform Module — Helm Install` under the existing `Reference` group with URI `github.com/seanoseanohay/bundle-analyzer-terraform-modules//modules/helm-install?ref=v0.3.35`. Hit a multi-stage authorization+state bug getting the customer-side page to render: (a) the Replicated GitHub App initially only had access to the content repo, so when EP first tried to fetch the new Terraform repo it 403'd; (b) attempting to grant per-repo access in the GitHub App settings, accidentally switched from "All repositories" to "Only select repositories" without re-selecting the existing content repo, which broke EP's content link and bounced the Vendor Portal back to the "Set Up Content" wizard; (c) re-granted access to both repos, re-linked content via the wizard, linked the Terraform repo via the Terraform tab, but the customer-side helm-install page sat on "Documentation is being generated" overnight (~16 hours). Verified via `gh api` that the tag was correctly pushed (`7bfe200`), all five module files were readable at the tag, and the content repo's `toc.yaml` URI form matched the docs example. Diagnosed as renderer-side stuck cache: the renderer woke up during the permissions-flap window, 403'd on file fetch, cached the failure state for the `(repo, ref)` pair, and didn't auto-retry when permissions were later restored. Fixed by committing a fresh `toc.yaml` change (switched `?ref=v0.3.35` → `?ref=main`) which generated a new toc-change event with a different `(repo, ref)` key that bypassed the stuck cache — page rendered within 60 seconds. Kept `?ref=main` as the final state: docs page tracks the latest module content, while the `v0.3.35` git tag is still in place for the proxy registry's customer-side `terraform init` flow which is keyed off release `version_label` → matching `v`-prefixed tag. Customer-side render at `bundle-analyzer.enterpriseportal.app/0.3.35/content/infrastructure/helm-install` shows: title `helm-install`, our README description verbatim, commit `7bfe200`, "Minimal Example" + "Production Example" HCL blocks (AI-generated by the EP renderer over our parsed inputs — not extracted from our README; AI hallucinated the GitHub org as `bundle-analyzer/...` instead of `seanoseanohay/...` and used the `git::https://...` source form rather than the `proxy.replicated.com/...` form customers actually need). Screenshot saved to `bootcamp-screenshots/6.6-terraform-module-helm-install.png`. Two alpha-AI imperfections logged in `friction-logs/week2/24-epv2-terraform-renderer-stuck-and-hallucinated-examples.md` (combines the stuck-renderer state bug with the AI-example hallucination issues — both are renderer-side, not content-side).

## Phase 6.8 — Enterprise Portal Self-Service Sign-Ups ✓ COMPLETE
Goal: A prospective customer can hit a public signup URL, create a trial account, verify their email, and have an active customer record provisioned automatically in the Vendor Portal with the correct channel, license type, expiration, and install methods — closing the loop from "URL we put on a marketing site" to "real customer who can install the app."
Deliverables: 0.3.35 promoted to the Stable channel as the first deliberate prod-grade promotion of this app; Self-Service Sign-Ups configured in the Vendor Portal EP tab (Stable channel, 30-day trial, Helm + Embedded Cluster install methods, custom `Company name` field); successful end-to-end signup → verification → customer-record-appears verification with screenshots.
Success: Visiting `https://bundle-analyzer.enterpriseportal.app/signup` from an unauthenticated browser shows the customized signup form; submitting it produces an email with a 12-digit verification code; entering the code creates an active customer record in the Vendor Portal `Customers` page with all configured defaults applied; the new customer can log into the EP and see install instructions for their entitled methods.
Built (2026-05-21): Promoted Unstable release 95 (chart `0.3.35`) to the Stable channel via `replicated release promote 95 3DSQCjEPbAivec8zp7hIlnKsesN --version 0.3.35 --release-notes "..."` — first deliberate Stable promotion of this app (Stable had been stuck at release 28 / `0.1.1` since early April, predating every tier 5+6 feature). Intentionally kept Stable promotion as a manual step rather than auto-CI: CI auto-promotes to Unstable + EC-Test on every commit for fast feedback, but Stable should require a human "yes this is production-ready" decision — that's the entire point of having multiple channels and the gate self-serve customers depend on. Opened Vendor Portal → Enterprise Portal → Self-Service Sign-Ups. Notable: **first tier 6 alpha feature that did NOT require an admin.replbiz.com grant** — the tab loaded immediately with full config UI, breaking the pattern set by 6.2 (Customer Emails / Grant), 6.3 (Security Center / Grant), 6.4 (EP Customization + GitHub App / Marc), and 6.6 (Terraform Modules / Marc). Configured: Default Channel = `Stable`, Expiration = 30 days, License Type = `Trial`, Install Methods = `Helm CLI` + `Embedded Cluster` (kURL and KOTS are EPv2-incompatible per 6.4's discovery and not offered as options here). Added one Custom Signup Field: `Company name` (placeholder `Acme Inc`, required). No Terms & Conditions URL (none drafted). No email-domain filtering. Signup URL exposed by EP: `https://bundle-analyzer.enterpriseportal.app/signup`. End-to-end test: opened that URL in an incognito window — the form rendered with our customizations including the auto-generated subtitle `"Start your 30 day trial"` (derived from the 30-day expiration config) and the custom `Company name` field above `Work email address`. Submitted with company `bobblawlawblog` and a personal Gmail address. Received the verification email with the 12-digit code; entered it; account confirmed. Switched to the normal logged-in Vendor Portal tab → `Customers` → found new customer `bobblawlawblog` with: `Trial license` badge, `Stable` channel badge, `Expires on 06/20/2026 (in 29 days)`, `Created on 05/21/2026`, `Embedded Cluster enabled`, License ID `3E2YuUdL09kI1xDIFoFpUvcguPb`, Enterprise Portal Status = `Always enabled`, Users = 1, Last accessed = May 21 2026 10:45am. Screenshots saved to `bootcamp-screenshots/6.8-signup-form.png` and `bootcamp-screenshots/6.8-customer-record.png`. First clean tier 6 task — no admin grants, no permission flaps, no AI hallucinations, no stuck renderers, no docs ambiguities; this is what "production-ready customer-facing feature" looks like in the Replicated platform when an alpha capability is actually GA-quality.

## Phase 6.9 — End-to-End Install via Enterprise Portal 🚫 BLOCKED ON REPLICATED EP REDEPLOY
Goal: Invite a customer to the Enterprise Portal. As that customer, follow the install instructions through to a running app, once for the Helm install path and once for the Embedded Cluster install path. Rubric tests that the portal instructions are accurate and complete enough for a real customer to follow.
Deliverables: Successful customer-side install for both paths, with screenshots; any content/customer-config fixes needed along the way.
Success: Customer-side EP install pages render the personalized install commands; running the commands brings up a working Bundle Analyzer instance for both install methods.
Status (2026-05-21 ⏸ paused): Customer-facing EP install pages at `bundle-analyzer.enterpriseportal.app/0.3.35/content/installation/helm`, `/installation/linux`, and `/updates/instances` all return *"Something Went Wrong / Server Components render error"* for any customer state. After ~3 hours of bisection (stripping helm.md to baseline → adding components back one at a time → verifying customer license install types via CLI → testing both an invited customer and a self-serve-signup customer → confirming the Vendor Portal admin renderer shows the same Helm install data correctly), pinged the Replicated EP maintainers with the diagnostic chain. Their response: *"the code we are using for the web app is probably months out of date."* Targeted redeploy expected in 1–2 days. Holding tier 6 until the redeploy lands. Full incident write-up in `friction-logs/week2/25-epv2-stale-web-app-deploy.md`. When the redeploy is live, the resume sequence is: (a) verify the current template `helm.md` (commit `46a14d8` in content repo) now renders; (b) restore the full custom helm.md content from commit `f0c2ce7` (with my 9-section walkthrough — *What you're installing* table, prerequisites, verification, frontend-access port-forward, etc.); (c) same restore for `linux.md`; (d) replace `updates/instances.md` with current content; (e) test the Helm install path end-to-end with a fresh customer; (f) test the EC install path on a fresh VM; (g) write up 6.9 ✓ COMPLETE.

## Phase 6.10 — Upgrade Instructions Without Downtime ⏸ BLOCKED ON 6.9
Status (2026-05-21): Cannot start until 6.9 unblocks — 6.10 tests upgrades via the same `<InstancesAndUpdates />` component family that's broken on the stale EP web app deploy. Resumes when 6.9 resumes.

